# Frontend — Observability Dashboard

Not started yet. The React + TypeScript + Vite dashboard is built in **Phase 10**
against the backend APIs defined in §27 of the project spec and the seed data
from Phase 9.

Placeholder directory so the monorepo layout (`backend/` + `frontend/`) exists
from Phase 1.
